{% extends 'basedash.html.twig' %}

{% block title %}TypeMateriels{% endblock %}

{% block body %}
    <h1>TypeMateriels</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_materiel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_materiel.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_materiels_index') }}">back to list</a>

    <a href="{{ path('app_type_materiels_edit', {'id': type_materiel.id}) }}">edit</a>

    {{ include('type_materiels/_delete_form.html.twig') }}
{% endblock %}
