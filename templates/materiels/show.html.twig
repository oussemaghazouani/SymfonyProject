{% extends 'basedash.html.twig' %}

{% block title %}Materiels{% endblock %}

{% block body %}
    <h1   style="font-size: 3rem; color: red; font-weight: bold; text-align: center; margin-bottom: 30px; text-decoration: underline;">Materiels</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ materiel.name }}</td>
            </tr>

           <tr>
                   <th>Type</th>
                   <td>{{ materiel.type ? materiel.type.name : 'Aucun type défini' }}</td>
           </tr>
        </tbody>
    </table>

    <a href="{{ path('app_materiels_index') }}"  class="btn btn-primary">back to list</a>

    <a href="{{ path('app_materiels_edit', {'id': materiel.id}) }}"  class="btn btn-primary">edit</a>

    {{ include('materiels/_delete_form.html.twig') }}
{% endblock %}
