{% extends 'basedash.html.twig' %}

{% block title %}TypeProduit{% endblock %}

{% block tablle %}
    <h1>TypeProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_produit.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_produitt_index') }}">back to list</a>

    <a href="{{ path('app_type_produitt_edit', {'id': type_produit.id}) }}">edit</a>

    {{ include('type_produitt/_delete_form.html.twig') }}
{% endblock %}
